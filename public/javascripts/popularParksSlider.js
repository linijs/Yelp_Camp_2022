const slider = document.getElementById("popularParks");

slider.addEventListener("touchstart", (e) => {}, { passive: true });

slider.addEventListener("touchend", () => {});

slider.addEventListener("touchmove", (e) => {}, { passive: true });
